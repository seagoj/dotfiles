#!/bin/zsh

#
# Links media to home directory based on $MEDIA env var
#
# USAGE:
#
#   $ media
#   # => links $MEDIA/<type> to $HOME/type

for type in books code documents Downloads games movies music pictures tv;
do
    rm $HOME/$type
    ln -s $MEDIA/$type $HOME/$type
done

