#!/bin/zsh
# @todo: extract current value to toggle

TOGGLE="TRUE"

if [[ $1 == 0 ]]; then
    TOGGLE="FALSE"
fi

echo $(`defaults read com.apple.finder AppleShowAllFiles`);
echo $TOGGLE

defaults write com.apple.finder AppleShowAllFiles $TOGGLE

killall Finder
