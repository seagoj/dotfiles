#!/bin/sh

app="$(basename ${0})"

sudo docker run -it --rm \
    -v `pwd`:/home/"${app}" \
    -v "${HOME}"/.config/"${app}":/home/"${app}"/.config/"${app}" \
    --name "${app}" \
    seagoj/docker-"${app}" \
    "${@}"
