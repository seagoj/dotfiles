dots=~/code/dotfiles/

for link in .Xresources .archey3.cfg .antigen .vimrc .vim .fonts .gitconfig .geeknote .zshrc antigen.zsh bin; do
    rm -R ~/$link
    ln -s $dots$link ~/$link
    echo $link linked
done
