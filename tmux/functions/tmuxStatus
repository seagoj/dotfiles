autoload tmuxSplit; tmuxSplit "$1"

case $OS_TYPE in
Arch)
    tmux send-keys "archey3 -c && wwwtxt" C-m
    ;;
Darwin | Mac)
    tmux send-keys "OSXey -c && wwwtxt" C-m
    ;;
*)
    echo "$OS_TYPE"
    ;;
esac
