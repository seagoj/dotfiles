#!/bin/sh
# vim: sw=4 ts=4 sts=4 noexpandtab

if [ "$1" = "--next" ]; then
	doc_name=`date --date=tomorrow "+%Y-%m-%d"`
else
	doc_name=`date "+%Y-%m-%d"`
fi

anthology "Daily Plan" "$HOME"/vimwiki/daily-plan "$doc_name"
