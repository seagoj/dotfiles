#!/bin/zsh
# vim: filetype=sh

if [[ $# -eq 0 ]]; then
    echo "Usage:"
    echo "aurInstall [...packages names]"
    exit 0
fi
autoload installFromAur

for var in "$@"; do
    installFromAur "$var"
done
