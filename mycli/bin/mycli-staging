#!/bin/bash

if [[ ! -f /var/log/mycli-staging.log ]]; then
    sudo touch /var/log/mycli-staging.log
    sudo chmod +rw /var/log/mycli-staging.log
fi

mycli\
    --host 192.168.0.47\
    --user root\
    --pass $BOT_SQL_PASS\
    --database bps\
    --logfile /var/log/mycli-staging.log
